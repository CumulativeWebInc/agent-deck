{
  "schema_version": "cwi.schema-versions/1.0",
  "registry": {
    "cwi.registry/1.0": {
      "file": "registry/registry.json",
      "description": "Public agent/product registry index — per-product capability metadata"
    },
    "cwi.ship-week/1.0": {
      "file": "ship-week/ship-week.json",
      "description": "Ship Week machine-readable plan — status, proposed dates, days, bounties, kill rule"
    },
    "cwi.schema/1.0": {
      "file": "schema.json",
      "description": "JSON-LD structured data (schema.org) for the Agent Deck storefront"
    }
  },
  "note": "New JSON files ship a schema_version and a registry entry in the same commit."
}
